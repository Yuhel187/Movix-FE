import PersonManagement from "@/components/admin/master-data/PersonManagement";

export const metadata = { title: "Quản lý Nhân sự | Admin" };

export default function PersonPage() {
  return <PersonManagement />;
}